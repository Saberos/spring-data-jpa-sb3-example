package com.example.demo.repository;

import com.example.demo.entities.EntityK;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryK extends JpaRepository<EntityK, Long> {
}
