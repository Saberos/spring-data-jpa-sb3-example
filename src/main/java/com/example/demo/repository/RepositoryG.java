package com.example.demo.repository;

import com.example.demo.entities.EntityG;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryG extends JpaRepository<EntityG, Long> {
}
