package com.example.demo.repository;

import com.example.demo.entities.EntityS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryS extends JpaRepository<EntityS, Long> {
}
