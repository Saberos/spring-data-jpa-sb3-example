package com.example.demo.repository;

import com.example.demo.entities.EntityD;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryD extends JpaRepository<EntityD, Long> {
}
