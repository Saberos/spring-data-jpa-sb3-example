package com.example.demo.repository;

import com.example.demo.entities.EntityV;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryV extends JpaRepository<EntityV, Long> {
}
