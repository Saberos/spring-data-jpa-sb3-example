package com.example.demo.repository;

import com.example.demo.entities.EntityW;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryW extends JpaRepository<EntityW, Long> {
}
